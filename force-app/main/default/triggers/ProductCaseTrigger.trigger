/**
 * @author: gmiranda
 * @date: 09/05/2018 
 * @service
 */
/**
* @service Hecho por NTS
*/
trigger ProductCaseTrigger on Product_Case__c (before insert, before update, before delete, after insert, after update, after delete) {

	// TriggerFactory.createHandler(Product_Case__c.sObjectType);
}
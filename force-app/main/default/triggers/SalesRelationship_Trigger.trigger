/**
* @date September 2021
* @group Triggers
* @deprecated We use SalesRelationshipTrigger instead
*/
trigger SalesRelationship_Trigger on Sales_Relationship__c (before insert, before update, before delete, after insert, after update, after delete) {
}
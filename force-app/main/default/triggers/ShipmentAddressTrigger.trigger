/**
 * @author: gmiranda
 * @date: 09/05/2018 
 */
/**
* @service Hecho por NTS
*/
trigger ShipmentAddressTrigger on Shipment_Address__c (before insert, before update, before delete, after insert, after update, after delete) {

	// TriggerFactory.createHandler(Shipment_Address__c.sObjectType);
}
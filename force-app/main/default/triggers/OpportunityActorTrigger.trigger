/**
* @deprecated January 2022 No se usa el componente de la clase
*/
trigger OpportunityActorTrigger on Opportunity_Actor__c (before insert, before update, before delete, after insert, after update, after delete)
{
}
/**
 * Dummy - Lo pide Copado porque esta calse existía pero aunque se borró,
 * Copado la mantiene en su package.xml
 */
@isTest public with sharing class SystemErrorNotifierTest {
   @isTest static void test001() {
    Assert.isTrue(true,'This test should always pass');
   }
}
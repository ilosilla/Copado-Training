<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>gmpkg__xInMemFilterPolicy__c</fullName>
    <defaultValue>&apos;ALL_URLS&apos;</defaultValue>
    <deprecated>false</deprecated>
    <description>Policy for applying interactive filters: 
 - ALL_URLS (persist across all pages), 
 - BY_URL (persist by URL)
 - NEVER (no persistence).</description>
    <externalId>false</externalId>
    <inlineHelpText>Choose how interactive filters should be applied: 
 - ALL_URLS (persist across all pages), 
 - BY_URL (persist by URL)
 - NEVER (no persistence)</inlineHelpText>
    <label>Interactive Filter Policy</label>
    <length>80</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>

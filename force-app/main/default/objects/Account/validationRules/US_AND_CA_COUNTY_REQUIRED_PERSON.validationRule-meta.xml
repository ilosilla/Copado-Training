<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>US_AND_CA_COUNTY_REQUIRED_PERSON</fullName>
    <active>true</active>
    <description>US and CA addresses require the STate for Sovos to calculate tax properly</description>
    <errorConditionFormula>AND (
CONTAINS($User.Username, &apos;loads.user&apos;) = false,
  IsPersonAccount = true,
  Potential__c = false,
  OR(
    ISPICKVAL(PersonMailingCountryCode, &apos;US&apos;), 
    ISPICKVAL(PersonMailingCountryCode, &apos;CA&apos;)
  ),
  ISPICKVAL(PersonMailingStateCode, &apos;&apos;)
)</errorConditionFormula>
    <errorMessage>State required for US and Canada accounts</errorMessage>
</ValidationRule>
